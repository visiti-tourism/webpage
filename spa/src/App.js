import {StyledApp} from "./App.styled.js";
import Routing from "./pages/routing/Router";

function App() {
    return (
        <StyledApp>
            <Routing></Routing>
        </StyledApp>
    );
}

export default App;
